---
name: NX POC Design System
description: 日通營運監控與調度管理 Prototype 的高密度桌面介面規範。
tokens:
  colors:
    primary: "#123D88"
    primary-dark: "#092A68"
    primary-light: "#E8F0FF"
    canvas: "#F4F7FA"
    surface: "#FFFFFF"
    surface-subtle: "#F8FAFC"
    text-primary: "#192434"
    text-secondary: "#667386"
    border: "#DDE4EC"
    success: "#1F8A5B"
    info: "#2F73C8"
    warning: "#D97917"
    error: "#C43D4B"
  typography:
    font-family: 'Inter, "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", sans-serif'
    page-title: "24px / 750"
    section-title: "16px / 700"
    body: "14px / 400"
    body-strong: "14px / 650"
    caption: "12px / 400"
  rounded:
    control: 8px
    card: 8px
    compact: 6px
    pill: 999px
  spacing:
    xs: 4px
    sm: 8px
    md: 12px
    lg: 16px
    xl: 24px
  components:
    button-height: 36px
    sidebar-width: 220px
    top-header-height: 56px
    table-row-min-height: 52px
    panel-default-width: 320px
---

## 1. Overview

本系統的視覺北極星是「調度控制台」：在有限的桌面空間中，讓調度人員快速掃描大量車輛、任務與例外狀況，並能在不中斷主工作脈絡的情況下完成查詢、指派、審核與修正。整體氣質應專業、清楚、可靠；品牌感主要由深藍側欄、藍色操作與一致的資訊密度建立，而不是依賴裝飾。

畫面層級固定為「全域導覽 → 頁面任務 → 列表或時間軸 → 右側詳情／對話框」。列表、時間軸與地圖都是主要工作表面，應最大化可視範圍；詳情、搜尋及編輯使用漸進揭露，避免在主畫面堆疊所有欄位。狀態不得只靠顏色，必須搭配文字、圖示或位置關係。

## 2. Colors

- `#092A68`：側欄與最深品牌背景，只用於全域導覽。
- `#123D88`：主要操作、選取、Focus、現在時間線與重要連結。
- `#2F73C8`：資訊、提早、定位 Focus 等次級藍色語意；避免與主要操作混用。
- `#F4F7FA`：頁面畫布；`#FFFFFF` 為主要工作表面；`#F8FAFC` 為表頭、Hover 與輕量分區。
- `#192434`：主要文字；`#667386`：輔助資訊；`#DDE4EC`：結構分隔與邊框。
- `#1F8A5B`：成功與準時；`#D97917`：預估風險或需注意；`#C43D4B`：已發生的延遲、錯誤或破壞性操作。

品牌藍只標記可操作、被選取或需要聚焦的內容。一般資料保持中性色，避免整頁同時出現大量高彩度區塊。Hover 使用淡灰或淡藍底；選中態以藍色外框、左側指示線或低透明度藍底表達。

## 3. Typography

全站使用 Inter 搭配繁體中文字型回退。頁面標題 24px／750；區塊標題 16px／700；表格欄名、重要欄位與按鈕 13–14px／650；一般內容 14px／400；時間、地址與輔助資訊 11–12px／400–600。

營運資料以可掃讀為優先：車號、站點與主要名稱可用半粗體，但不應將整張卡片或整列內容全部加粗。狀態與時間差採短語，例如「延遲 15 分」、「預估抵達 16:30」。長地址允許換行；固定欄寬中的名稱使用單行省略，完整內容由 Hover 或詳情揭露。

## 4. Elevation

介面採低陰影、以邊框建立結構。主要卡片使用 `1px #DDE4EC` 邊框與 `0 1px 2px rgba(22,34,52,.05)`；Hover 或輕量浮層可使用 `0 4px 12px rgba(22,34,52,.08)`；Tooltip、Dialog 與浮動面板最高使用 `0 8px 24px rgba(22,34,52,.10)`。

同層級區塊不靠陰影互相切割，使用共享邊線與無縫拼接。右側工作面板位於導覽列下方，與主內容平行；Dialog 才使用遮罩。Focus 不以加深大面積底色處理，優先使用清晰外框、指示線或局部高亮。

## 5. Components

- 側欄：220px 深藍背景，項目高至少 44px；選中使用同色系亮底，圖示與文字保持 12px 間距。
- 頂部導覽：56px 白底、底部細線，僅保留全域識別與通知等必要操作。
- 按鈕：高度至少 36px、8px 圓角。主要動作使用深藍實心；次要動作使用藍色外框或文字；破壞性確認使用紅色。
- 表格：52px 以上列高、表頭淡灰底。Hover 僅改變淺色底，選取列以藍色結構線顯示。批次操作只有選取後才出現。
- 狀態：一般狀態用文字＋小圖示；Tag 僅用於需要快速辨識且具有封閉集合的狀態，不用於一般描述。
- 右側面板：承接檢視、快速編輯與連續處理；標題與固定操作置頂，主要操作固定於底部，內容區獨立捲動。
- Dialog：用於新增、刪除確認與會中斷工作脈絡的任務；寬度依內容控制，長表單需限制高度並保留固定頁尾。
- Tooltip：補充被截斷或次要資訊，不承載完成流程所必需的唯一操作。
- 表單：Label 置於欄位上方；必填、唯讀與錯誤狀態需明確。選項數少時使用 Radio，多選或大量選項使用 Select／Autocomplete。

## 6. Do's and Don'ts

### Do's

- 優先顯示例外、待處理數量與可採取的下一步。
- 讓主列表、時間軸與右側詳情共享同一筆資料與選取狀態。
- 使用分頁、篩選與批次操作支援大量營運資料。
- 保留鍵盤 Focus、按鈕替代操作與足夠的觸控／點擊範圍。
- 以 4、8、12、16、24px 間距系統維持緊湊但不擁擠的節奏。

### Don'ts

- 不把品牌色當成大面積裝飾，也不在同一畫面堆疊多種高彩度狀態。
- 不用過大的卡片、圓角或留白犧牲營運資訊密度。
- 不讓 Hover、拖曳或顏色成為完成任務的唯一方式。
- 不將搜尋、編輯與詳細欄位永久攤開在主畫面。
- 不用模糊的「AI 推薦」取代可驗證的時間、資源與衝突依據。
